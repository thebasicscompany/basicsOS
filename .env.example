# Basics OS Environment Variables
# Run `pnpm dev:setup` to auto-generate this file, or fill in manually.

# ── Required ───────────────────────────────────────────────────────────────────

DATABASE_URL=postgresql://basicos:basicos_dev@localhost:5432/basicos
REDIS_URL=redis://localhost:6379
BETTER_AUTH_SECRET=    # generate: openssl rand -hex 32
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# ── AI features — choose one option ───────────────────────────────────────────

# Option A: Managed API key from basicsos.com
# Covers all AI models (Claude, GPT-4, embeddings), TTS, and STT.
# No provider accounts needed. Get a key at https://basicsos.com/keys
# AI_API_KEY=bsk_live_...
# AI_API_URL=https://api.basicsos.com

# Option B: Bring your own API keys
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# ── Email delivery (optional) ─────────────────────────────────────────────────
# Invite links are logged to console if not set. Free tier: https://resend.com
# RESEND_API_KEY=re_...
# EMAIL_FROM=noreply@yourdomain.com

# ── Meeting transcription (optional) ─────────────────────────────────────────
# Free tier available at https://deepgram.com
# DEEPGRAM_API_KEY=...

# ── Real-time collaboration (optional) ───────────────────────────────────────
# Start the Hocuspocus server first (packages/sync)
# NEXT_PUBLIC_COLLAB_URL=ws://localhost:4001

# ── Production Docker (required when using docker-compose.prod.yml) ───────────
# POSTGRES_PASSWORD=change-me-in-production
