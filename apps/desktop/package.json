{
  "name": "@basicsos/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "BasicOS desktop app (Electron v33)",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc && electron dist/main/index.js",
    "build": "tsc --noEmit && echo 'build ok'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist out"
  },
  "devDependencies": {
    "@basicsos/config": "workspace:*",
    "@types/node": "^22.0.0",
    "electron": "^40.4.1",
    "electron-builder": "^26.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
