{
  "name": "basicsos",
  "private": true,
  "version": "0.1.0",
  "description": "BasicOS — Open-source company operating system",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "db:migrate": "pnpm --filter @basicos/db migrate",
    "db:seed": "pnpm --filter @basicos/db seed",
    "db:studio": "pnpm --filter @basicos/db studio",
    "gen:module": "plop",
    "setup": "tsx scripts/setup-wizard.ts",
    "dev:setup": "bash scripts/dev-setup.sh",
    "db:generate": "pnpm --filter @basicos/db generate"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "plop": "^4.0.1",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "overrides": {
      "ioredis": "5.4.0"
    }
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
