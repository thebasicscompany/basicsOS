{
  "name": "@basicsos/auth",
  "version": "0.0.1",
  "private": true,
  "description": "BasicOS authentication with Better Auth",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@basicsos/db": "workspace:*",
    "better-auth": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@basicsos/config": "workspace:*",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
