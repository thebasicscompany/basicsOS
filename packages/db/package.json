{
  "name": "@basicsos/db",
  "version": "0.0.1",
  "private": true,
  "description": "BasicOS database schema and migrations",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "bun": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "generate": "bunx drizzle-kit generate",
    "migrate": "bunx drizzle-kit push && bun src/apply-rls.ts",
    "migrate:sql": "bunx drizzle-kit generate && bunx drizzle-kit migrate && bun src/apply-rls.ts",
    "rls": "bun src/apply-rls.ts",
    "studio": "bunx drizzle-kit studio",
    "seed": "bun src/seed.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "drizzle-orm": "^0.45.1",
    "drizzle-zod": "^0.7.0",
    "pg": "^8.13.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@basicsos/config": "workspace:*",
    "dotenv": "^16.4.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.6.0",
    "@types/pg": "^8.11.0"
  }
}
