import { z } from "zod";
import { TRPCError } from "@trpc/server";
import { router, protectedProcedure, memberProcedure } from "../trpc.js";

export const {{camelCase name}}Router = router({
  list: protectedProcedure.query(async ({ ctx }) => {
    if (!ctx.tenantId) throw new TRPCError({ code: "UNAUTHORIZED" });
    // TODO: implement list query
    return [];
  }),

  create: memberProcedure
    .input(z.object({ name: z.string().min(1) }))
    .mutation(async ({ ctx, input }) => {
      // TODO: implement create mutation
      return { id: "placeholder", tenantId: ctx.tenantId, ...input };
    }),
});
